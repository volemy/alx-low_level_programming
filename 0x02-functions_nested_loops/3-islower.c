#include "main.h"
/**
 * _islower - program function that checks for lowercase letters
 *
 * Description: function checks whether the given character is a lowercase
 *               It returns 1 if the character is lowercase, otherwise 0)
 * @c: is the character to be checked
 *
 * Return: 1 if characteer is lowercase, otherwise 0
 *
 */
int _islower(int c)
{
	int char c

	if (c >= 97 && c <= 122)

		return (1);

	else
		return (0);

}
