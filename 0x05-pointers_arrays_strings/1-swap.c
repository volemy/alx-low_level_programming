#include "main.h"
#include <stdio.h>
/**
 * swap_int - function that swaps the values of two integers
 * @a: first integer to swap
 * @b: second integer to swap
 *
 * Description: check if function swaps the integers values
 * Return: 0 (success)
 */
void swap_int(int *a, int *b)
{
	int temp value = *a;
	*a = *b;
	*b = temp value;
}

