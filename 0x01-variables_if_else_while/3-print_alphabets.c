#include <stdio.h>
/**
 * main- Entry point
 *
 * Return: 0 (success)
 */
int main(void)
{
	int ch = 'a';

	while (ch <= 'z')
	{
	putchar(ch);
	ch += 1;
	}

	int ch = 'A'

	while (ch <= 'Z')
	{
	putchar(ch);
	ch += 1
	}

	putchar('\n');
	return (0);
}
